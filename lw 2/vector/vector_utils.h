#pragma once

#include "stdafx.h"

using namespace std;

void PrintVector(ostream & output, const vector<double> & numbers);

vector<double> GetNumbers(istream & input);

void Vector—onversion(vector<double> & numbers);